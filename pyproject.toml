[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cloud-fusion"
dynamic = ["version"]
description = "Unified CLI tool for streamlined cloud operations"
readme = "README.md"
requires-python = ">=3.11,<3.14"
license = "MPL-2.0"
license-files = ["LICENSE"]
authors = [
    { name = "Snigdhajyoti Ghosh", email = "cloud-fusion.support@snigji.com" },
]
keywords = [
    "cloud",
    "cloud-cli",
    "multi-cloud",
    "aws",
    "aws-sdk",
    "aws-cli",
    "aws-authentication",
    "aws-sdk-python",
    "aws-auth",
    "gcp",
    "gcloud",
    "google-cloud",
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Intended Audience :: Developers",
    "Intended Audience :: System Administrators",
    "Operating System :: OS Independent",
    "Development Status :: 5 - Production/Stable",
    "Topic :: Utilities",
]
dependencies = [
    "boto3>=1.43.18",
    "pyperclip>=1.11.0",
    "keyring>=25.7.0",
    "beautifulsoup4>=4.14.3",
    "requests>=2.34.2",
    "inquirer>=3.4.1",
]

[project.urls]
Homepage = "https://github.com/snigdhasjg/cloud-fusion"
Repository = "https://github.com/snigdhasjg/cloud-fusion"

[project.scripts]
cloud-fusion = "cloud_fusion.app:main"

[tool.hatch.version]
path = "cloud_fusion/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["cloud_fusion"]

[tool.hatch.build.targets.wheel.shared-scripts]
"bin/_awsp" = "_awsp"
"bin/_awsp.ps1" = "_awsp.ps1"
"bin/_awsr" = "_awsr"
"bin/_awsr.ps1" = "_awsr.ps1"
"bin/_gcpc" = "_gcpc"
"bin/_gcpc.ps1" = "_gcpc.ps1"
"bin/_gcpr" = "_gcpr"
"bin/_gcpr.ps1" = "_gcpr.ps1"

[tool.hatch.build.targets.sdist]
include = [
    "cloud_fusion",
    "bin",
    "README.md",
    "LICENSE",
]
